{% extends 'base.html.twig' %}
{% block nav %}
<nav>
    <a href="{{ path('accueil') }}"><i class="bi bi-house"></i></a>
    <a href="{{ path('carte') }}"><i class="bi bi-geo-alt"></i></a>
    <a href="{{ path('app_forum_sujet_index') }}"><i class="bi bi-chat-dots"></i></a>
    {% if is_granted('ROLE_ADMIN') %}
    <a href="{{ path('admin') }}"><i class="bi bi-gear"></i></a>
    {% endif %}
    <a href="{{ path('login') }}"><i class="bi bi-box-arrow-in-left"></i></a>
</nav>
{% endblock %}
{% block title %}Modifier un Parcours{% endblock %}

{% block body %}
    <h1>Modifier un Parcours</h1>
    <div class="form-container">
    {{ include('parcours/_form.html.twig', {'button_label': 'Enregistrer'}) }}
    {{ include('parcours/_delete_form.html.twig') }} 
    </div>
    <a href="{{ path('app_parcours_index') }}" class="link-back">Retour à la liste</a>
{% endblock %}

{% block footer %}
<div class="foot">
    <hr>
    <p>Mentions légales - © 2025 - ALAND DRINNHAUSEN SCHMITT</p>
</div>
{% endblock %}